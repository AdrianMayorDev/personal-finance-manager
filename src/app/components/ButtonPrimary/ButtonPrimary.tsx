import styles from "./ButtonPrimary.module.scss";

const { primaryButton } = styles;

const buttonPrimary = () => {
	return <button className={primaryButton}>Placeholder</button>;
};

export default buttonPrimary;
